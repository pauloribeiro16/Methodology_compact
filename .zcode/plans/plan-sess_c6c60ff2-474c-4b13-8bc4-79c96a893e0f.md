# Reorganização dos ficheiros de instruções de agente — Methodology_compact

## Contexto
O repo compacto (1.214 ficheiros: `00_METHODOLOGY/` com 918, `02_CASES/` com 295) é um corpus documental sem tooling. Os AGENTS.md atuais referenciam ~90% de caminhos inexistentes (scripts/, .hooks/, REFERENCE/, TOOL_REGISTRY.md, 01_IMPLEMENTATION_TOOLS/, CONTEXT/, etc.) e prescrevem um branch workflow impossível (não há git). Decisões do utilizador: iniciar git agora; manter modelo Orchestrator→Executor/Validator + P0–P7; âmbito = AGENTS.md + briefs; trabalho principal = continuar os casos Case_01–03.

## 1. Reescrever `AGENTS.md` da raiz (~100–120 linhas, inglês — consistente com o corpus)
Estrutura nova:
- **System Identity** — Orchestrator + Executor/Validator (mantém).
- **What this repo is** — corpus de trabalho compacto da metodologia AEGIS; nota explícita: lints, evals, `kg.sh`, hooks e scripts vivem no repo principal e **não estão disponíveis aqui** (validação de documentos faz-se no repo principal ou manualmente).
- **Repository map** — só o que existe: `00_METHODOLOGY/` (`PREPROCESSING_by_domain/` com domains D-01…D-10 + CONTROLS + MAPPINGS, `00_NIS2_Mapping/`, `00_VISUALISATIONS/`, `diagrams/`, `dependency_graph.yaml`) e `02_CASES/` (3 casos × fases 1–3; nota da inconsistência de nomenclatura: Case_01 usa `03_PHASE3_DECOMPOSITION_RICH`, Cases 02/03 usam `03_PHASE3_DECOMPOSITION`).
- **Where to start (casos)** — cadeia de entrada: `02_CASES/GLOBAL_PROJECT_STATE.md` → `Case_0X/PROJECT_STATE.md` + `progress.json` → `PROJECT_STATE.md` de cada fase. Aviso de que `02_CASES/README.md` e `GLOBAL_PROJECT_STATE.md` contêm referências a estrutura do repo principal (não editar, ler com filtro).
- **Design Principles P0–P7** — mantidos na íntegra; P5 adaptado: sem `kg.sh`, usar `00_METHODOLOGY/dependency_graph.yaml` + grep para impacto de mudanças em IDs `SR-*`/`SO-*`/`RULE-*`/`REQ-*`/`D-XX.Y`.
- **Pre-Flight Checklist** — adaptado: confirmar caso/fase, ler state files da cadeia acima, consultar dependency_graph.yaml se tocar IDs. Remove checks de branch, imports aegis_phase1 e pytest.
- **Version control (novo, mínimo)** — commits por unidade de trabalho com formato `[AGENT] <scope>: <action> — <case>` (herdado do AGENTS.md do 00_METHODOLOGY); sem branch policy pesada.
- **Key documents index** — substitui a Reference Index morta por ficheiros reais: `dependency_graph.yaml`, `diagrams/README.md`, `02_CASES/GLOBAL_PROJECT_STATE.md`, `PREPROCESSING_by_domain/domains/index.md`, `PARSE_DOMAIN_EXECUTION_BRIEF.md`.

Remove: Branch Workflow + Branch Policy completos, hooks/CI, Reference Index de 16 ficheiros fantasmas, TOOL_REGISTRY.md, pre-flight de aegis_phase1/pytest, menções a `.agents/` legado.

## 2. Reescrever `00_METHODOLOGY/AGENTS.md` (~80–100 linhas)
- Mantém o que tem valor scoped: **ID hierarchy** (SO/AO/PO/RULE/REQ + deprecated PG/SG/CR/BPR), **boundaries** Always/Ask/Never adaptadas, code style mínimo (existem 18 .py).
- **Project Structure** corrigida para as 4 subpastas reais + estrutura `D-0X.Y` (D-0X.Y.md + manifest + articles/).
- **Diagram rules**: substitui as ~28 linhas duplicadas por pointer para `diagrams/README.md`.
- Remove: tabela de skills, comandos de lint/KG (ferramentas inexistentes), secção Git Workflow (passa para a raiz), referências a CONTEXT/, QUALITY/, GUIDES/, TEMPLATES/, REFERENCE/, .venv.

## 3. Corrigir referências mortas em `PARSE_DOMAIN_EXECUTION_BRIEF.md` (edição mínima)
- Marcar `00_METHODOLOGY/SCHEMA/obligated_party.yaml`, `01_IMPLEMENTATION_TOOLS/evals_draft/markdown_parser.py` e `01_IMPLEMENTATION_TOOLS/lints/structural/lint_document_structure.py` como residentes no repo principal (indisponíveis aqui) — sem alterar o restante conteúdo do brief.
- Nota: a deduplicação dos `TEMPLATE_subagent_brief.md` deixou de ser necessária (o utilizador eliminou ambas as cópias na última redução).

## 4. Inicializar git
- `git init` + `.gitignore` mínimo (`__pycache__/`, `*.pyc` — não apaga nada, só exclui do tracking).
- Commit 1: estado atual antes das edições (snapshot do corpus).
- Commit 2: os ficheiros de instruções reescritos.

## 5. Validação final
- Verificar que todos os caminhos referenciados nos novos AGENTS.md existem (extração de paths + `test -e`).
- Confirmar orçamento de linhas (raiz ≤120, 00_METHODOLOGY ≤100).

## Fora do âmbito (por decisão do utilizador)
- Não editar `02_CASES/README.md`, `GLOBAL_PROJECT_STATE.md`, `CHANGE_LOG_CENTRAL.md` (apenas avisos no AGENTS.md sobre estarem desatualizados).
- Não remover anomalias (`__pycache__/`, path aninhado duplicado em 00_VISUALISATIONS, diretórios com nome `*.md`) — decisões de ficheiros são do utilizador.