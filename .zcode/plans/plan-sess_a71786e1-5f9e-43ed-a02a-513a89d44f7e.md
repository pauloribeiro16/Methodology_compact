# Campanha: UML DIAGRAMS — use case diagrams (actor+ovals) nos catálogos + sequence diagrams para anexo B

## Decisões locked (2026-09-05)
1. **Notação**: Mermaid `useCaseDiagram` (beta, requer Mermaid ≥ v11.6 — nota no topo de cada anexo; GitHub actual renderiza; VS Code antigo pode falhar). Stick figures nativos + ovals + system boundary. Nota de fallback: quem não renderizar vê o código-fonte (legível).
2. **Granularidade**: 1 diagrama **system-wide** por caso + 1 diagrama **por package** (C1: PKG-7..11 + PKG-DP..TRN; C2: PKG-8..12; C3: PKG-A..F) ≈ 25 diagramas novos. Actors vindos dos cards (Primary Actor + Stakeholders); include/extend críticos das Extensions.
3. **Sequence diagrams**: os **80 existentes** (23 C1 + 26 C2 + 31 C3) extraídos dos catálogos para `annexes/B_Sequence_Diagrams.md` por caso; cada card fica com uma linha `**Sequence diagram:** → Annex B §N`. Compliance PROC/CAP cards não levam sequence diagrams nesta fase ("não é para esta altura") — os seus diagramas são os flowcharts §5C.4 que já têm.
4. **Âmbito**: os 3 casos.

## Fase 0 — Convenção (rubrica v1.7 §5C.5, ~15 linhas)
- UML use case diagrams: system-wide + per package, Mermaid useCaseDiagram, source = catalogue cards (Primary Actor → actor; card title → oval; Extensions → include/extend).
- Sequence diagrams: **annex-only** a partir de agora (Annex B por caso, um por product UC card, ordenado por id); catálogos ficam com pointer de 1 linha.
- Nota de renderização Mermaid ≥ v11.6.

## Fase 1 — Script de extracção (`scripts/extract_sequence_diagrams.py`)
- Para cada caso: percorre o catálogo, apanha cada ` ```mermaid\nsequenceDiagram…``` ` block + o card header `#### UC…/PROC…` que o precede; move o block para `annexes/B_Sequence_Diagrams.md` como `## §N — <card id + title>`; substitui no catálogo por `**Sequence diagram:** → Annex B §N (B_Sequence_Diagrams.md)`.
- Idempotente; reporta n extraídos por doc; nunca toca diagrams não-sequence (flowcharts §5C.4 ficam onde estão — são dos lane cards, não dos catálogos).

## Fase 2 — Use case diagrams (por caso, Executor)
- **C1**: converter `annexes/A_Use_Case_Diagrams.md` (2 diagramas graph-style) para `useCaseDiagram` + acrescentar per-package (PKG-7..11, PKG-DP..TRN) = ~12 diagramas.
- **C2**: manter o §5.1 system-wide existente (mover para o anexo A se fizer sentido — não, fica no catálogo; anexo A stub populado com per-package PKG-8..12) = ~6 diagramas novos.
- **C3**: anexo A stub populado: 1 system-wide + 6 per-package (PKG-A..F) = ~7 diagramas.
- Fonte: Primary Actor/Stakeholders/Extensions dos cards do catálogo (fiel).

## Fase 3 — Verificação
- 25 use case diagrams estruturalmente válidos; 80 sequence diagrams no anexo B (contagem 23/26/31); catálogos com 0 `sequenceDiagram` inline e pointers 1-linha; 3 gates PASS; audit 100/100/100 mantido (doc tocados = catálogos/anexos, sem tocar em ids).

## Fase 4 — Bookkeeping
- Ledger §3 (lane artefacts: diagrams DONE); CHANGE_LOG 1.5; GLOBAL §4J; version bumps nos catálogos + anexos; 4 commits (rubrica+script, C1, C2, C3) + memória.

## Critérios de aceitação
- ~25 useCaseDiagrams (stick figures) nos anexos A; 80 sequenceDiagrams nos anexos B; catálogos sem sequenceDiagram inline; 3 gates PASS.

## Riscos
- **Mermaid ≥ v11.6**: nota de compatibilidade no topo dos anexos; fallback visual = código legível.
- **Extracção cirúrgica de 80 blocos**: script com contagem exacta + dry-run.
- **Anexo A C1**: conversão substitui os 2 diagramas graph-style (git preserva histórico).