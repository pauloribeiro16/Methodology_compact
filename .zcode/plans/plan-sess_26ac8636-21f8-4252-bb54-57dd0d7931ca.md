# R7 — Mais informação nas tabelas + modal navegável + folios vazios colapsáveis

Decisões locked (tu, 2026-09-09): **3 melhorias nas tabelas/modal** + **ocultar Folios 4/11 vazios**.

## R7.1 — Mais colunas nas tabelas (no `AEGIS_Master_Dashboard.html`)

Editar os 9 call-sites de `ensureDT` para adicionar 1-3 colunas a cada um, lendo `obj.detail.*` ou `obj.*` directo:

- **folio02-amb-table**: adicionar column `Type` (`obj.type`) — schema tem `type` no JSON.
- **folio05-rules-table**: adicionar `NI` column já tem, mas adicionar `Anchor` (concat `obj.anchors[0]` se existe, ou `—`).
- **folio06-obj-table**: adicionar `Type` column (`obj.framework` já tem; adicionar `obj.type` se existir, senão `—`).
- **folio09-uc-table**: adicionar `Threats` column (`(obj.threats_addressed || []).join(", ")` — verificar chave).
- **folio10-proc-cap-table**: adicionar `Activities` column (preview das primeiras 40 chars).
- **folio11-req-table**: adicionar `Type` column (FR/NFR) + `Description` preview (primeiros 60 chars).
- **folio12-threat-table**: adicionar `Threat` column (preview 50 chars) + `Type` column.
- **folio-p2-ambiguity-table**: já existe; adicionar `Type` column.
- **folio-p3-threat-ambiguity-table**: adicionar `Likelihood` + `Impact` columns.

Adicionalmente: **filtros laterais em algumas tabelas** (já existem em alguns; adiciono onde faltam):
- folio09: filtro por Lane ✓ (já existe)
- folio10: filtro por Lane ✓
- folio11: filtro por Type (FR/NFR)
- folio12: filtro por Severity (já existe via `bindSelectFilter` em algumas)

## R7.2 — Mini-TOC + filtro no modal

Adicionar no `showDetailModal`:
- **TOC fixo** no canto superior direito do modal (só se `obj.detail.sections.length > 3`): lista de links para cada secção; click faz scroll suave para a secção.
- **Filtro de texto** acima do body: input que filtra secções por título OU conteúdo (oculta secções que não correspondem ao termo).

CSS: `.am-toc`, `.am-filter` (sticky top, fundo `--paper-2`, fonte mono pequeno).

JS: na render, criar um `<nav class="am-toc"><ul>` no header (se >3 secções) e um `<input class="am-filter">` no topo do body. Listener que filtra por `section.title.toLowerCase().includes(q) || section.body.toLowerCase().includes(q)`.

## R7.3 — Folios vazios colapsáveis

Adicionar lógica: quando um Folio tem **só placeholders** (todos os KPIs "--" e tabela "No data available in table"), adicionar class `.am-folio-empty` que:
- CSS: opacity 0.45, pointer-events none, posição compacta (height colapsado para 80px)
- Banner visível: "Folha vazia — dashboard dedicado: <link>"
- O link aponta para `unified/AEGIS_Dashboard.html` para Folio 4 (cascade) ou `Case_0X_P1_Dashboard.html` para Folio 11.

Implementação: em cada renderP*, adicionar uma função `isFolioEmpty(tableId, kpiIds)` que verifica se todas as KPIs são "--" E a tabela tem 0 rows. Se sim, aplica a class.

Folios afectados:
- **Folio 4 Graph view** — tem sempre canvas real (vis-network), não é vazio. Não colapsar.
- **Folio 11 Requirements & Allocation** (P3) — em C1 com 30+46 = 76 rows, não é vazio. Em C2/C3 também populado. Pode acontecer vazio em casos sem docs FR/NFR — raro.
- **Folio 7 Tensions & Posture** (P2) — sempre populado (4 tensões × 3 casos). Não é vazio.

Em vez de esconder genérico, **adicionar um link "Ver dashboard dedicado"** nos Folios 4, 6, 7, 8, 11, 12 (todos os P2/P3 Folios) que aponta para o dashboard especializado correspondente — mesmo se não vazios. Isso resolve o "estes dados não tem muitos dados" no sentido de "tem mais fora do Master Dashboard".

## R7.4 — Tooltips nas colunas

Adicionar atributo `title=` em cada `<th>` das 9 tabelas com uma descrição amigável da coluna:
- "ID" → "Unique rule identifier (e.g. CR-D-01.1-001)"
- "Title" → "Human-readable rule/UC name"
- "Realization" → "How the rule is realized: TECHNOLOGY (artefact) / PROCESS (SOP) / CAPABILITY (standing ability)"
- "NI" → "Normative Intensity (0–4 scale)"
- "Source" → "Where the rule was derived from (yaml explicit, heuristic-derived, or cross-link)"
- etc.

Também: tooltips nos KPI cards (data-f5="tech" → "Rules classified as built-in artefact controls").

## R7.5 — Testes

Estender `tests/test_master_drilldown.py`:
- R7.1: validar que cada tabela tem pelo menos 1 nova column com conteúdo real (não "--").
- R7.2: abrir modal com 5+ secções; validar `.am-toc` presente e clicável; digitar no filtro e verificar que secções desaparecem.
- R7.3: validar que `unified/AEGIS_Dashboard.html` está ligado nos Folios 6/8/11/12.

Smoke 17/17 + master 15/15 + drilldown 12/12 mantidos.

## R7.6 — Bookkeeping

- CHANGE_LOG 8.1→8.2, GLOBAL 8.6→8.7, ledger §1J.
- Commit: `[EXECUTOR] R7 master-dashboard richer-tables: extra columns + mini-TOC + filter + tooltips + cross-dashboard links`.

## Invariantes
- Sem mexer nos 02_CASES/**, validation/, kg/**, build_control_set.py, progress.json.
- 1 subagente + verificação minha.
- Smoke e master 15/15 mantidos.