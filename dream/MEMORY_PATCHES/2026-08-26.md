# Memory Patch — 2026-08-26 (Dream nightly)

## Patches proposed (do not auto-apply)

### 1. `tooling-gap.md` — SkillNet MCP scope is now user, not workspace

**Reason:** Commit `ca5808c` (2026-08-26) moved the `skillnet` MCP server entry
from `<repo>/.zcode/config.json` (workspace-scope) to
`~/.zcode/cli/config.json` (user-scope) so the MCP is available across
all workspaces. The memory file still says "registered workspace-scope
in `.zcode/config.json`".

**Proposed edit** in `~/.zcode/cli/memories/projects/methodology_compact-1839a9d8746b19ef/memory/tooling-gap.md`:

```diff
- MCP server `~/.local/share/skillnet-mcp` registered workspace-scope in `.zcode/config.json` (8 tools: health_check, search_skills, download_skill, import_best_skill, get_skill_rules, create_skill, evaluate_skill, analyze_skills)
+ MCP server `~/.local/share/skillnet-mcp` registered **user-scope** in `~/.zcode/cli/config.json` (8 tools: health_check, search_skills, download_skill, import_best_skill, get_skill_rules, create_skill, evaluate_skill, analyze_skills)
```

### 2. `tooling-gap.md` — `originSessionId` should reflect both sessions of 2026-08-26

**Reason:** Today's work spanned two ZCode sessions on the compact repo:
`sess_c6c60ff2-474c-4b13-8bc4-79c96a893e0f` (this session — orchestrator)
and `sess_f3830651-2f51-4532-a321-402dc38cf195` (user-side session that
produced the Sprint 6 case work, KG ontology extension, and P1 dashboard
rebuild). The memory file currently attributes the snapshot to the
first only.

**Proposed edit:**

```diff
- originSessionId: sess_c6c60ff2-474c-4b13-8bc4-79c96a893e0f
+ originSessionIds:
+   - sess_c6c60ff2-474c-4b13-8bc4-79c96a893e0f   # orchestrator
+   - sess_f3830651-2f51-4532-a321-402dc38cf195   # user-side Sprint 6
```

(If the memory schema requires a single string, leave the primary session
and add the second to a `cross-references` field instead.)

## Patches NOT proposed (verified current)

- `kg.sh` paths and subcommand list in tooling-gap.md — still accurate.
- 8-tool list for the SkillNet MCP — verified live today
  (handshake JSON-RPC returned the same 8 tools).
- `case-context-loader` / `doc-conventions` symlink paths — still correct
  (verified `~/.zcode/skills/`).
- Hooks (`kg-reminder`, `brief SessionStart`, `guard-protected-files`,
  `guard-bash`) — all in the correct scope per `ca5808c` (workspace).

## P7 note

Per the dream contract (P7 non-negotiable), these are proposed patches
for the human to apply. The dream does NOT edit the memory store
directly. The human reviews and either applies the diffs above (Edit
tool on the memory file) or rejects them.

## Provenance

- Generated by dream automation `automation-c073db97-...` (cron `55 23 * * *`).
- Triggered after 2026-08-26 commits `c94b840` (rename), `8d1f752` (harness),
  `538c77e` (web), `e514328` (branding), `ca5808c` (config split).
