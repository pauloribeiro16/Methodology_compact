# Memory Patch — 2026-08-28 (Dream nightly)

## Proposed edits (do not auto-apply — P7)

### 1. `tooling-gap.md` — SkillNet MCP scope is workspace, but the repo says user

**Reason:** Line 24 of tooling-gap.md still says the SkillNet MCP server is
`registered workspace-scope in `.zcode/config.json``. Commit `ca5808c`
(2026-08-26) moved the SkillNet MCP entry from `<repo>/.zcode/config.json`
(workspace) to `~/.zcode/cli/config.json` (user) so the MCP is available
across all workspaces. Today's Dream nightly audit confirmed the live
state — the user-scope install is the one that responded to the JSON-RPC
handshake.

**Proposed edit** in `~/.zcode/cli/memories/projects/methodology_compact-1839a9d8746b19ef/memory/tooling-gap.md`:

```diff
-- MCP server `~/.local/share/skillnet-mcp` registered workspace-scope in `.zcode/config.json` (8 tools: health_check, search_skills, download_skill, import_best_skill, get_skill_rules, create_skill, evaluate_skill, analyze_skills)
+- MCP server `~/.local/share/skillnet-mcp` registered **user-scope** in `~/.zcode/cli/config.json` (8 tools: health_check, search_skills, download_skill, import_best_skill, get_skill_rules, create_skill, evaluate_skill, analyze_skills)
```

### 2. `MEMORY.md` — pointer to the grill-me-devils-advocate-plan patch is stale

**Reason:** Yesterday's patch (`MEMORY_PATCHES/2026-08-27-grill-me-plan-mode.md`)
proposed flipping `grill-me-devils-advocate-plan.md` from PENDING to
DONE. Today, the file already shows `grill-me` in the list and MEMORY.md
references it correctly. Nothing to do — the user applied yesterday's
patch (or skipped it; the deliverable at `~/.zcode/commands/grill-me.md`
exists regardless).

**No edit proposed.**

## P7 note

Per the dream contract (P7 non-negotiable), these are proposed patches
for the human to apply. The dream does NOT edit the memory store
directly. The human reviews and either applies the diffs above (Edit
tool on the memory file) or rejects them.

## Provenance

- Generated by dream automation `automation-c073db97-...` (cron `55 23 * * 1-5`).
- Triggered after 2026-08-28 commits (latest: `b0cddb6` Case_03 port campaign
  Fase 7 complete — PRODUCTION_FLOW v1.0 + flow audit PASS_WITH_NOTES).
- Today's audit shows: 50 KG calls (last 7d), 5 WEAK items including
  `mcp:skillnet` (0/sem) — the patch above aligns memory with the
  documented state.
